"use client";

/**
 * Il sito non usa attualmente analytics, pixel pubblicitari o cookie opzionali.
 * Il banner resta quindi disattivato per evitare di chiedere un consenso non necessario.
 * Se in futuro verranno aggiunti strumenti di tracciamento, questo componente potrà
 * essere esteso per bloccarli fino a una scelta valida dell'utente.
 */
export function CookieConsent() {
  return null;
}
